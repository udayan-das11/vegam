<%= form_for(@item) do |f| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :category %><br>
    <%= f.select :category, [['Fertilize', 'Fertilize'], ['Pesticide', 'Pesticide'], ['Tool', 'Tool'], ['Machinery', 'Machinery']],{ include_blank: true }, { class: 'transac_type' } %>
  </div>
  <div class="field">
    <%= f.label :uom %><br>
    <%= f.select :uom, [['Litre', 'Litre'], ['Kg', 'Kg'], ['Unit', 'Unit']],{ include_blank: true }, { class: 'transac_type' } %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>